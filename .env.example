DATABASE_URL=postgres://user:password@host:5432/dbname
# The public origin. Used for invitation links and Stripe redirects.
APP_URL=https://autotriz.com.bd
AUTH_SECRET=
STRIPE_SECRET_KEY=
R2_ENDPOINT=
R2_ACCESS_KEY=
R2_SECRET_KEY=
R2_BUCKET=
R2_PUBLIC_URL=
# Outgoing email for staff invites. Without these, an invite still works —
# the admin is given a link to pass on by hand.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=
